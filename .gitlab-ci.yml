stages:
  - test
  - build

variables:
  NODE_VERSION: "20"

cache:
  paths:
    - node_modules/

lint_and_typecheck:
  stage: test
  image: node:20-alpine
  script:
    - npm ci || npm install
    - npm run build
